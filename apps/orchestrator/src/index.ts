/**
 * Orchestrator Application
 *
 * Workflow graph engine and routing decisions.
 * Determines which agent executes a task, manages execution flow.
 *
 * @module @chatbot/orchestrator
 * @stage 02-infrastructure (full implementation)
 */
export const ORCHESTRATOR_VERSION = '0.0.1' as const
