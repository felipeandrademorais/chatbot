/**
 * Worker Application
 *
 * Specialized agent execution and tool invocation.
 * Processes tasks from the queue, calls LLM providers and tools.
 *
 * @module @chatbot/worker
 * @stage 02-infrastructure (full implementation)
 */
export const WORKER_VERSION = '0.0.1' as const
