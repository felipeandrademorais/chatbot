/**
 * Gateway Application
 *
 * HTTP entry point for the chatbot platform.
 * Responsibilities: request validation, job creation, response delivery.
 *
 * @module @chatbot/gateway
 * @stage 02-infrastructure (full implementation)
 */
export const GATEWAY_VERSION = '0.0.1' as const
