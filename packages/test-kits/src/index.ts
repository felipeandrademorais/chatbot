/**
 * Test Kits Package
 *
 * Shared test utilities, fixtures, builders, and helpers
 * used across all test suites (unit, integration, e2e).
 *
 * @module @chatbot/test-kits
 */
export const TEST_KITS_VERSION = '0.0.1' as const
