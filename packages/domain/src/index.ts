/**
 * Domain Package
 *
 * Pure business logic — framework-independent.
 * Contains aggregates, use cases, repositories, and domain invariants.
 *
 * @module @chatbot/domain
 * @stage 03-core-domain (full implementation)
 */
export const DOMAIN_VERSION = '0.0.1' as const
