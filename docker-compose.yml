services:
  postgres:
    image: postgres:16-alpine
    container_name: chatbot-postgres
    restart: unless-stopped
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-chatbot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-chatbot_dev}
      POSTGRES_DB: ${POSTGRES_DB:-chatbot}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-chatbot} -d ${POSTGRES_DB:-chatbot}']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - chatbot-net

  redis:
    image: redis:7-alpine
    container_name: chatbot-redis
    restart: unless-stopped
    ports:
      - '${REDIS_PORT:-6379}:6379'
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    networks:
      - chatbot-net

  # --- Placeholder Application Services ---
  # These will be fully configured in Stage 02 (Infrastructure).
  # Uncomment and configure when Dockerfiles are available.

  # gateway:
  #   build:
  #     context: ./apps/gateway
  #     dockerfile: Dockerfile
  #   container_name: chatbot-gateway
  #   restart: unless-stopped
  #   ports:
  #     - '${GATEWAY_PORT:-3000}:3000'
  #   environment:
  #     NODE_ENV: ${NODE_ENV:-development}
  #     POSTGRES_HOST: postgres
  #     POSTGRES_PORT: 5432
  #     POSTGRES_USER: ${POSTGRES_USER:-chatbot}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-chatbot_dev}
  #     POSTGRES_DB: ${POSTGRES_DB:-chatbot}
  #     REDIS_HOST: redis
  #     REDIS_PORT: 6379
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   networks:
  #     - chatbot-net

  # worker:
  #   build:
  #     context: ./apps/worker
  #     dockerfile: Dockerfile
  #   container_name: chatbot-worker
  #   restart: unless-stopped
  #   environment:
  #     NODE_ENV: ${NODE_ENV:-development}
  #     POSTGRES_HOST: postgres
  #     POSTGRES_PORT: 5432
  #     POSTGRES_USER: ${POSTGRES_USER:-chatbot}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-chatbot_dev}
  #     POSTGRES_DB: ${POSTGRES_DB:-chatbot}
  #     REDIS_HOST: redis
  #     REDIS_PORT: 6379
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   networks:
  #     - chatbot-net

volumes:
  postgres-data:
    driver: local
  redis-data:
    driver: local

networks:
  chatbot-net:
    driver: bridge
